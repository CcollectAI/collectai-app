/**
 * Marketplace API client.
 *
 * Previously contained 11 exported functions (listings, buyer intents,
 * agreements, ratings, chat) and 7 associated types that were never
 * imported anywhere in the codebase. They were removed during dead-code
 * cleanup (G1). Re-add endpoints here when the Deal Desk feature lands.
 */
