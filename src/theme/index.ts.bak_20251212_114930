export * from './colors';
export * from './tokens';

import { colors, radius, spacing, shadow } from './tokens';
export const theme = { colors, radius, spacing, shadow };

export { useAppTheme } from './useAppTheme';

/**
 * Back-compat alias: older files import { useTheme } from "@/theme"
 * We point it to the same hook so nothing breaks.
 */
export { useAppTheme as useTheme } from './useAppTheme';
