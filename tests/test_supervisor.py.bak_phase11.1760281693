import asyncio
import pytest
from app.tasks.supervisor import _retry_task

@pytest.mark.asyncio
async def test_retry_task_runs_successfully():
    called = {"ok": False}
    async def mock_task():
        called["ok"] = True
    await _retry_task(mock_task, "mock_task")
    assert called["ok"] is True
