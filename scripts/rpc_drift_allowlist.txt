# RPC drift allowlist. Each non-comment line is matched literally
# against entries from `scripts/audit_fe_rpc_drift.py`:
#
#   MISSING_FN:  <file>:MISSING_FN:<rpc_name>
#   BAD_PARAM:   <file>:BAD_PARAM:<rpc_name>:<param>
#
# Removing a line MUST be paired with fixing the underlying drift.

# 2026-04-30 — rpc_convert_watchlist_to_item_v1 entry REMOVED.
# convertWatchlistToItem was rewritten in watchlistProvider.ts to
# compose the conversion at the boundary using existing endpoints
# (read v_watchlist_items_v1 → POST /items → DELETE /watchlist/mine/{id}).
# No new RPC needed.
