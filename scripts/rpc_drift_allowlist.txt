# RPC drift allowlist. Each non-comment line is matched literally
# against entries from `scripts/audit_fe_rpc_drift.py`:
#
#   MISSING_FN:  <file>:MISSING_FN:<rpc_name>
#   BAD_PARAM:   <file>:BAD_PARAM:<rpc_name>:<param>
#
# Removing a line MUST be paired with fixing the underlying drift.
# Adding a line is a deliberate "we know this is broken; deferred".

# ─────────────────────────────────────────────────────────────────────
# 2026-04-29 — initial sweep allowlist trimmed: most entries previously
# parked here were FE callers wired to non-existent Supabase RPCs.
# The providers were redirected to working EC2 routes (/watchlist/mine,
# /events/*, /events/categories/*/follow, /items, etc.) so most
# allowlist lines were removable. The two below remain because they
# need server-side work before the FE wire makes sense.
# ─────────────────────────────────────────────────────────────────────

# convert-watchlist-to-item: no equivalent EC2 route or Supabase RPC.
# UI button still shows; tapping it throws "Could not find the function".
# Decide: drop the button OR add a server endpoint that creates an item
# from a watchlist row + deletes the watchlist row in one transaction.
src/data/providers/watchlistProvider.ts:MISSING_FN:rpc_convert_watchlist_to_item_v1
