import React from 'react';
import { View, Text } from 'react-native';
import { useAppTheme } from "@/hooks/useAppTheme";

export default function PortfolioScreen() {
  const t = useAppTheme();
  const colors = (t as any)?.colors ?? (t as any);

  return (
    <View style={{ flex: 1, padding: 16, backgroundColor: colors?.tiffany ?? colors?.background ?? '#e7fbff' }}>
      <Text style={{ fontSize: 18, fontWeight: '700', color: colors?.navy ?? '#0b1f3a' }}>
        Portfolio baseline is loading ✅
      </Text>
      <Text style={{ marginTop: 8, color: colors?.navy ?? '#0b1f3a' }}>
        Next step: re-introduce your real Portfolio UI incrementally from a known-good backup.
      </Text>
    </View>
  );
}
