import { AddQuickScanLayout } from '@/components/AddQuickScanLayout';
import React from "react";
import {
  SafeAreaView,
  ScrollView,
  View,
  Text,
  StyleSheet,
  Pressable,
} from "react-native";
import { Ionicons } from "@expo/vector-icons";
import { Link } from "expo-router";
import { useAppColors } from "../ui/theme";
import { PortfolioHoverChart } from "@/components/PortfolioHoverChart";

const AddScreen: React.FC = () => {
  const colors = useAppColors();

  
  const [name, setName] = React.useState("");
  const [estimatedValue, setEstimatedValue] = React.useState("");
  const [notes, setNotes] = React.useState("");

return (
  <AddQuickScanLayout
    name={name}
    setName={setName}
    estimatedValue={estimatedValue}
    setEstimatedValue={setEstimatedValue}
    notes={notes}
    setNotes={setNotes}
    lastScanSummary={lastScanSummary}
    onQuickScan={handleQuickScan}
    onSave={handleSave}
  />
);


        {/* Manual entry (real Supabase) */}
        <Link href="/add-manual" asChild>
          <Pressable
            style={[
              styles.card,
              { backgroundColor: colors.card, borderColor: colors.border },
            ]}
          >
            <View style={styles.cardIconCircle}>
              <Ionicons name="create-outline" size={18} color={colors.accent} />
            </View>
            <View style={{ flex: 1 }}>
              <Text style={[styles.cardTitle, { color: colors.text }]}>
                Manual entry (live)
              </Text>
              <Text style={[styles.cardBody, { color: colors.muted }]}>
                Type in a card, figure or kit manually. Saves directly into your
                portfolio_items table in Supabase so it can show up in Items
                and Analytics later.
              </Text>
            </View>
            <Ionicons
              name="chevron-forward-outline"
              size={18}
              color={colors.muted}
            />
          </Pressable>
        </Link>

        {/* Build & paint projects (real Supabase) */}
        <Link href="/build-paint-projects" asChild>
          <Pressable
            style={[
              styles.card,
              { backgroundColor: colors.card, borderColor: colors.border },
            ]}
          >
            <View style={styles.cardIconCircle}>
              <Ionicons
                name="color-palette-outline"
                size={18}
                color={colors.accent}
              />
            </View>
            <View style={{ flex: 1 }}>
              <Text style={[styles.cardTitle, { color: colors.text }]}>
                Build & paint tracker (live)
              </Text>
              <Text style={[styles.cardBody, { color: colors.muted }]}>
                Track backlog, active projects and completed builds. Logging
                time here writes to build_paint_sessions and feeds your
                Analytics screen.
              </Text>
            </View>
            <Ionicons
              name="chevron-forward-outline"
              size={18}
              color={colors.muted}
            />
          </Pressable>
        </Link>

        {/* Fraud check */}
        <Link href="/fraud-check" asChild>
          <Pressable
            style={[
              styles.card,
              {
                backgroundColor: colors.card,
                borderColor: colors.border,
                marginBottom: 24,
              },
            ]}
          >
            <View style={styles.cardIconCircle}>
              <Ionicons
                name="shield-checkmark-outline"
                size={18}
                color={colors.accent}
              />
            </View>
            <View style={{ flex: 1 }}>
              <Text style={[styles.cardTitle, { color: colors.text }]}>
                Fraud check (mock)
              </Text>
              <Text style={[styles.cardBody, { color: colors.muted }]}>
                Basic checklist / notes around authenticity, card stock and
                seller history. For now it&apos;s a structured notes flow; later
                we can hook it into pricing / risk flags.
              </Text>
            </View>
            <Ionicons
              name="chevron-forward-outline"
              size={18}
              color={colors.muted}
            />
          </Pressable>
        </Link>
};

const styles = StyleSheet.create({
  safeArea: { flex: 1 },
  scroll: { flex: 1 },
  scrollContent: {
    paddingHorizontal: 16,
    paddingVertical: 12,
    gap: 10,
  },
  headerRow: {
    flexDirection: "row",
    justifyContent: "space-between",
    alignItems: "flex-start",
    marginBottom: 12,
  },
  headerLabel: {
    fontSize: 12,
    textTransform: "uppercase",
    letterSpacing: 0.5,
    fontWeight: "600",
  },
  headerTitle: {
    fontSize: 22,
    fontWeight: "700",
    marginTop: 2,
  },
  headerSub: {
    fontSize: 12,
    marginTop: 4,
    maxWidth: 280,
  },
  headerIcon: {
    width: 32,
    height: 32,
    borderRadius: 16,
    alignItems: "center",
    justifyContent: "center",
    backgroundColor: "#FFFFFF",
    borderWidth: 1,
    borderColor: "#D6E4EC",
  },
  card: {
    flexDirection: "row",
    alignItems: "center",
    borderRadius: 12,
    borderWidth: 1,
    paddingHorizontal: 12,
    paddingVertical: 12,
    gap: 10,
  },
  cardIconCircle: {
    width: 32,
    height: 32,
    borderRadius: 16,
    alignItems: "center",
    justifyContent: "center",
    backgroundColor: "#E7F6F8",
  },
  cardTitle: {
    fontSize: 15,
    fontWeight: "600",
    marginBottom: 2,
  },
  cardBody: {
    fontSize: 12,
  },
});

export default AddScreen;
