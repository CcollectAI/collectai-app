.PHONY: fmt lint typecheck test metrics logs

fmt:
	black .
	ruff check . --fix

lint:
	ruff check .
	black --check .

typecheck:
	mypy app

test:
	pytest -q

metrics:
	curl -s http://127.0.0.1:8080/metrics | head

logs:
	sudo journalctl -u collectors-merge.service -n 20 --no-pager
