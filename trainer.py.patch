*** trainer.py.prefeat.bak	2025-09-29
--- trainer.py	2025-09-29
@@
-from pipelines.utils import load_dataset, save_artifact
+from pipelines.utils import load_dataset, save_artifact
+from typing import Dict, Any, Callable
+
+def _get_featurizer(category: str) -> Callable[[Dict[str, Any]], Dict[str, float]]:
+    c = (category or "").strip().lower()
+    if c == "diecast":
+        from app.features.diecast import featurize as f
+        return f
+    if c == "lego":
+        from app.features.lego import featurize as f
+        return f
+    # default: identity (e.g., pokemon already numeric-ready)
+    return lambda x: x
@@
-    X_train = ds["train"]["features"]
+    feat = _get_featurizer(args.category)
+    X_train = [feat(row) for row in ds["train"]["features"]]
@@
-    X_val = ds["val"]["features"]
+    X_val = [feat(row) for row in ds["val"]["features"]]
